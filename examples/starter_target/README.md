# Starter Target

This example is the smallest target-owned shape that still exercises the full
local Tinkerloop loop:

- one target app entrypoint
- one traced tool function
- one adapter factory
- one scenario file

Use it when you want to understand the minimum integration surface before
adapting a real target repo.
